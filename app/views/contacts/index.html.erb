<h1>Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, contact_path(contact) %></li>
  <% end %>
</ul>

<p><%= link_to "New Contact", new_contact_path %></p>

<p><%= link_to "New Mass Text", new_mass_text_path %></p>
